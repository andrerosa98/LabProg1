/*
import java.util.Scanner;

public class L01Ex30 
{
    public static void main (String[] args)
    {
        Scanner input = new Scanner(System.in);

        float velocidade;
        float angulo;
        float alcance;
        final float gravidade = 9.8

        System.out.print("Digite a velocidade inicial (m/s): ");
        velocidade = input.nextFloat();
        System.out.print("Digite o ângulo entre o canhão e o solo: ");
        angulo = input.nextFloat();

        alcance = ((float)Math.pow(velocidade,2) / gravidade) / Math.sin(angulo);

        System.out.printf("O alcance do projétil será de %.2f metros.", angulo);
    }    
}
*/
